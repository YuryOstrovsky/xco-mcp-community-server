__pycache__/
*.pyc
.env
.env.*
!.env.example
# Python
__pycache__/
*.pyc
*.pyo

# Virtual environments
.venv/
venv/


# Archives / scratch
archive/
*.zip

# Secrets
.env
*.sh

# OS junk
.DS_Store

# Archives / legacy
archive/
xco-mcp-archive/

# Generated artifacts
*.zip
# Intermediate catalog-build outputs (regenerable; only mcp_tools.json,
# mcp_capabilities.json, services.json are tracked + needed at runtime).
generated/endpoints.json
generated/final_endpoints.json
generated/resolved_endpoints.json
generated/validated_endpoints.json
generated/*.bak

# Python noise
__pycache__/
*.pyc
*.pyo
*.pyd

# Virtualenv
.venv/
venv/

# Local scratch / helper scripts (NOT the pytest suite — `tests/` is tracked).
# NOTE: do not use a bare `test*` here; it silently ignores `tests/` and
# `test_*.py`, hiding the unit-test suite from the repo and CI.
xco_get_token.py
mcp-server.log

# Runtime logs (keep folder structure, ignore actual log files)
logs/*
!logs/.gitkeep

# Smoketest results
smoke-test/*.json
smoke-test/*.txt

# Internal dev plan — kept locally, never published
COMMUNITY_BACKPORT_PLAN.md

# Python build artifacts
dist/
build/
*.egg-info/
